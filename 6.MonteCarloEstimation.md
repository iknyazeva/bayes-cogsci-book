# Part 6.1: Monte Carlo estimation

Monte Carlo estimation replaces an integral with an average over simulated draws. It is the simplest computational idea in this session and the basis for interpreting posterior draws produced by more complex algorithms.

## 1. The estimand is a function of the parameters

Let $\theta^{(1)},\ldots,\theta^{(S)}$ be independent draws from a distribution $p(\theta)$. For any integrable function $g$,

$$
\mu_g=\mathbb E[g(\theta)]
\approx
\widehat\mu_g=\frac{1}{S}\sum_{s=1}^{S}g(\theta^{(s)}).
$$

Different choices of $g$ answer different questions:

| Question | Function of a draw |
|---|---|
| What is the mean? | $g(\theta)=\theta$ |
| What is $P(\theta>c)$? | $g(\theta)=\mathbb I(\theta>c)$ |
| What is a predicted outcome? | draw $\widetilde y^{(s)}\sim p(\widetilde y\mid\theta^{(s)})$ |
| What is a regional contrast? | $g(\theta)=\mu_A(\theta)-\mu_B(\theta)$ |
| What is a nonlinear policy summary? | evaluate the full transformation for every draw |

This draw-by-draw transformation is safer than inserting posterior means into a nonlinear function: in general, $\mathbb E[g(\theta)]\neq g(\mathbb E[\theta])$.

## 2. A benchmark with known truth

Suppose $\theta\sim\operatorname{Gamma}(a=2,\text{rate}=1/3)$. Its exact mean is $a/\text{rate}=6$. NumPy parameterises the Gamma distribution by **scale**, the reciprocal of rate:

```python
import numpy as np

rng = np.random.default_rng(2026)
draws = rng.gamma(shape=2, scale=3, size=10_000)
estimate = draws.mean()
mcse = draws.std(ddof=1) / np.sqrt(draws.size)
```

The estimate should be close to 6, but it will not equal 6 exactly. That difference is simulation noise. The example is useful because an analytic answer lets us test the code.

*Figure 6.1: Monte Carlo Running Means. 20 simulated chains converging to the exact Gamma mean $E[\theta]=6.0$ inside the theoretical $95\%$ MCSE envelope $\pm 1.96 \frac{\text{SD}}{\sqrt{S}}$:*

```{raw} html
<iframe src="_static/fig6_1_mc_running_mean.html" width="100%" height="370px" style="border:none; border-radius:8px;"></iframe>
```

```{admonition} Parameterisation check
:class: warning
Never write only “Gamma$(a,b)$” in computational material. State whether $b$ is a rate or scale, and verify the software convention.
```

## 3. Monte Carlo standard error

Under the usual finite-variance central-limit conditions,

$$
\operatorname{MCSE}(\widehat\mu_g)
\approx \frac{s_g}{\sqrt S},
$$

where $s_g$ is the sample standard deviation of $g(\theta^{(s)})$. MCSE measures the numerical uncertainty of the estimate. It does not measure posterior uncertainty about $\theta$.

Useful checks include:

- plot the running estimate against the number of draws;
- repeat the entire simulation under several seeds;
- compare empirical error with the predicted $1/\sqrt S$ rate;
- report MCSE for the substantive estimand, not only for base parameters.

For MCMC draws, replace $S$ by an estimand-specific effective sample size. The elementary formula based on summed autocorrelations is useful for intuition, but modern software should estimate ESS robustly.

## 4. Probabilities as averages of indicators

For any event $A$,

$$
P(\theta\in A)=\mathbb E[\mathbb I(\theta\in A)]
\approx \frac{1}{S}\sum_{s=1}^{S}\mathbb I(\theta^{(s)}\in A).
$$

For example, the posterior probability that an intervention improves standardized trust by more than 0.10 is the proportion of draws satisfying $\delta^{(s)}>0.10$.

```python
prob_meaningful = np.mean(delta_draws > 0.10)
```

This is not the same as the probability that the intervention will improve trust for every individual. The latter is a predictive question requiring a model for individual outcomes.

Rare-event probabilities require particular care. If no event occurs among 2,000 draws, the probability has not been proved to be zero; the simulation may simply be too short or poorly targeted for that tail.

## 5. Quantiles and intervals

Posterior interval endpoints are sample quantiles:

```python
lower, median, upper = np.quantile(delta_draws, [0.025, 0.50, 0.975])
```

Quantiles are nonlinear summaries. Their Monte Carlo error cannot generally be obtained by substituting the posterior standard deviation into the mean formula. Use tail ESS and software estimates of quantile MCSE, or a suitable batch/bootstrap approximation when working with independent draws.

## 6. Predictive simulation is nested Monte Carlo

Posterior prediction has two layers:

1. draw plausible parameters $\theta^{(s)}$ from $p(\theta\mid y)$;
2. draw a replicated or future outcome $\widetilde y^{(s)}$ from $p(\widetilde y\mid\theta^{(s)})$.

For a survey model, one draw may contain region effects, regression coefficients, and residual variation. A predicted respondent score should retain all relevant layers rather than use only the posterior mean regression line.

This separates:

- uncertainty about parameters;
- variation among future observations conditional on parameters;
- Monte Carlo error from using a finite number of draws.

## 7. Illustrations for this subchapter

The implementation note for Session 6 specifies reproducible scripts for the following figures:

1. **Running mean:** 25 simulated trajectories converging toward the exact Gamma mean.
2. **Square-root law:** log–log plot of empirical absolute error and MCSE against $S$, with slope $-1/2$.
3. **Probability as area:** Gamma density with the region $\theta>8$ shaded and the corresponding indicator average displayed.
4. **Nonlinear transformation:** compare $\mathbb E[\operatorname{logit}^{-1}(\eta)]$ with $\operatorname{logit}^{-1}(\mathbb E[\eta])$.
5. **Nested prediction:** display parameter uncertainty and outcome variation as separate layers.

Every figure should show the random seed, number of draws, exact benchmark where available, and the software parameterisation.

## 8. Guided practice

````{dropdown} Question: Quadrupling simulation draws
You quadruple the number of independent posterior draws ($S \to 4S$). What happens to the width of the posterior distribution, and what happens to the MCSE of its estimated mean?

**Answer.** The posterior distribution itself does not change ($p(\theta \mid y)$ is fixed by the model and data). The Monte Carlo standard error of the estimated mean is halved ($\operatorname{MCSE} \propto 1/\sqrt{S}$), provided the relevant variance is finite.
````

### Applied task

For posterior draws of a treatment effect $\delta$, calculate:

- the posterior mean $\mathbb{E}[\delta \mid y]$;
- $P(\delta > 0)$;
- $P(\delta > 0.10)$;
- a $95\%$ equal-tail credible interval;
- $\operatorname{MCSE}$ for each probability using the indicator draws.

Explain which quantities describe the posterior (epistemic uncertainty about parameters) and which describe numerical accuracy (Monte Carlo error).

## 9. Sources

The simulation-as-integration framework follows {cite}`jackman2009bayesian` and {cite}`kruschke2015doing`. The emphasis on estimands, simulation checks, and predictive quantities is aligned with {cite}`gelman2021regression` and {cite}`mcelreath2020statistical`.
