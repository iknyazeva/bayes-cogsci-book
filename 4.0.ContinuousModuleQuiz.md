# Big quize for continuous distribution

```{admonition} Instructions
This quiz covers Bayesian inference with continuous distributions: Normal (known and unknown variance), Exponential-Gamma conjugacy, non-informative priors, and Jeffreys priors. Show your calculations where applicable. For Python tasks, include your code and output.
```

## Recap Bayes

**Question 1.** Which of the following is one major difference between the frequentist and Bayesian approach to modeling data?
*   Frequentist models require a guess of parameter values to initialize models while Bayesian models require initial distributions for the parameters.
*   Frequentist models are deterministic (don't use probability) while Bayesian models are stochastic (based on probability).
*   Frequentists treat the unknown parameters as fixed (constant) while Bayesians treat unknown parameters as random variables.
*   The frequentist paradigm treats the data as fixed while the Bayesian paradigm considers data to be random.


## Exponential likelihood with gamma prior: Fixation Durations


**Question 2.** A researcher studies fixation durations during a reading comprehension task. She models the time spent on each fixation as exponential with rate $\lambda$ (fixations per second). She uses the conjugate Gamma prior for $\lambda$. Her prior belief is that the average fixation lasts about 250 ms, corresponding to a rate of $\lambda = 4$ fixations/second, with a standard deviation of 1 fixation/second. Given that the prior follows a $\text{Gamma}(a, b)$ distribution with $a = 16$, determine the value of $b$.

**Question 3.** Suppose that we wish to use a prior with the same mean ($\lambda = 4$), but with an effective sample size of just one observation. Then the prior for $\lambda$ is $\text{Gamma}(1, 0.25)$. The researcher records $n = 5$ fixation durations (in seconds): $Y_1 = 0.22, Y_2 = 0.35, Y_3 = 0.18, Y_4 = 0.41, Y_5 = 0.29$.

- What is the posterior distribution?
- What is the mean of the posterior distribution?

**Question 4.** Use Python to find the posterior probability that the average fixation duration exceeds 300 ms (i.e., $P(\lambda < 1/0.3) = P(\lambda < 3.33)$).


## Inter-Spike Intervals in Neural Recordings

The exponential distribution is often used to model the time between neural spikes (inter-spike intervals, or ISIs). When a neuron fires at an approximately constant rate, the ISIs can be modeled as exponentially distributed.

A neuroscientist records from a neuron in the visual cortex of an awake monkey during sustained visual stimulation. She models the ISIs as: $Y_i \sim \text{iid} \, \text{Exponential}(\lambda)$, where $E(Y) = 1/\lambda$ seconds. She uses a conjugate prior $\lambda \sim \text{Gamma}(a, b)$ and sets her prior expectation for the firing rate at $\lambda = 30$ spikes/second, with a prior effective sample size of one ISI.

**Question 5.** What is the value of $a$?

**Question 6.** What is the value of $b$?

**Question 7.** She records the following spike times (in seconds from stimulus onset): 0.012, 0.045, 0.061, 0.098, 0.130, 0.148, 0.179, 0.210, 0.244. What is the data vector of inter-spike intervals?

**Question 8.** What is the posterior distribution for $\lambda$?

**Question 9.** Use Python to calculate the upper end of the 95% equal-tailed credible interval for $\lambda$ (the firing rate in spikes per second).

**Question 10.** The posterior predictive density for a new inter-spike interval $y^*$ (in seconds) is:

$$
f(y^* \mid y) = \frac{\beta^\alpha \alpha}{(\beta + y^*)^{\alpha+1}} I\{y^* \geq 0\}
$$

where $\alpha$ and $\beta$ are the posterior Gamma parameters. Plot this posterior predictive density.


## Normal data

**Question 11.** Suppose we have a statistical model with unknown parameter $\theta$, and we assume a normal prior $\theta \sim N(\mu_0, \sigma_0^2)$, where $\mu_0$ is the prior mean and $\sigma_0^2$ is the prior variance. What does increasing $\sigma_0^2$ say about our prior beliefs about $\theta$?
*   Increasing the variance of the prior **narrows** the range of what we think $\theta$ might be, indicating **greater** confidence in our prior mean guess $\mu_0$.
*   Increasing the variance of the prior **narrows** the range of what we think $\theta$ might be, indicating **less** confidence in our prior mean guess $\mu_0$.
*   Increasing the variance of the prior **widens** the range of what we think $\theta$ might be, indicating **less** confidence in our prior mean guess $\mu_0$.
*   Increasing the variance of the prior **widens** the range of what we think $\theta$ might be, indicating **greater** confidence in our prior mean guess $\mu_0$.

**Question 12.** Suppose you are calibrating an EEG amplifier by measuring the voltage it reports when a known 10 µV calibration signal is applied. Due to electronic noise, you take $n$ independent measurements to estimate $\theta$, the mean voltage reading. Assume a normal likelihood for these data, with mean $\theta$ and known variance $\sigma^2 = 0.25$ µV² (standard deviation of 0.5 µV). Suppose your prior for $\theta$ is the conjugate normal. You expect the amplifier to be well-calibrated, so you set the prior mean at $m_0 = 10$ µV.

1. If you specify a prior variance $s_0^2$ for $\theta$, write the model for your measurements $Y_i, i = 1, \ldots, n$.
2. You decide you want the prior to be equivalent (in effective sample size) to one measurement. What value should you select for $s_0^2$? Round your answer to two decimal places.



**Question 13.** EEG amplifier calibration (continued).
You collect the following $n = 5$ measurements (in µV): (9.46, 9.54, 9.62, 9.49, 9.59).
What is the posterior distribution for $\theta$?


1. Use Python to find the upper end of a 95% equal-tailed credible interval for $\theta$.
2. After collecting these data, is it reasonable to conclude that the amplifier has a systematic bias (reads below the true 10 µV signal)?
3. What is the posterior predictive distribution of a single future measurement $Y$?

**Question 14.** Stroop Interference Effect

A cognitive psychologist measures Stroop interference (the RT difference between incongruent and congruent trials) for two age groups. She assumes a normal likelihood for the data with unknown mean $\mu$ and unknown variance $\sigma^2$. She uses the Normal-Inverse-Gamma conjugate prior where, conditional on $\sigma^2$, the prior for $\mu$ is normal with mean $m$ and variance $\sigma^2 / w$, and the marginal prior for $\sigma^2$ is $\text{Inverse-Gamma}(a, b)$.

Her prior guess for the mean interference effect is 100 ms, so $m = 100$. She is not very confident, so she sets $w = 0.1$. She sets $a = 3$ and $b = 5000$.

1. Simulate a large number of draws (at least 300) from the prior for $\sigma^2$ and report your approximate prior mean from these draws.

2. For the older adult group, she tests $n = 25$ participants and calculates sample mean $\bar{y} = 142$ ms and sample variance

$$
s^2 = \frac{1}{n - 1} \sum (y_i - \bar{y})^2 = 2800
$$

Using the update formulas for the Normal-Inverse-Gamma model, calculate the posterior distributions for the parameters.

$$
\sigma^2 \mid y \sim \text{Inverse-Gamma}(a', b')
$$

$$
\mu \mid \sigma^2, y \sim N\left(m', \frac{\sigma^2}{w + n}\right)
$$

Perform the posterior simulation and compute the approximate 95% equal-tailed credible interval for $\mu$.

3. For the young adult group, she tests $n = 30$ participants with sample mean $\bar{y} = 85$ ms and sample variance

$$
s^2 = \frac{1}{n - 1} \sum (y_i - \bar{y})^2 = 1600
$$

Treating the two groups as independent, compute the posterior probability $P(\mu_{\text{old}} > \mu_{\text{young}})$. Does the data support the hypothesis that older adults show a larger Stroop interference effect?


## Priors recap


**Question 15.** Suppose we flip a coin five times to estimate $\theta$, the probability of obtaining heads. We use a Bernoulli likelihood for the data and a non-informative (and improper) Beta(0,0) prior for $\theta$. We observe the following sequence: (H, H, H, T, H). Because we observed at least one H and at least one T, the posterior is proper. What is the posterior distribution for $\theta$?

**Question 16.** Continuing the previous question, what is the posterior mean for $\theta$? Round your answer to one decimal place.

**Question 17.** Consider again the EEG amplifier calibration problem.
Assume a normal likelihood with unknown mean $\theta$ and known variance $\sigma^2 = 0.25$. Now use the non-informative (and improper) flat prior for $\theta$ across all real numbers. This is equivalent to a conjugate normal prior with variance equal to $\infty$.

- You collect the following $n = 5$ measurements (in µV): $(9.46, 9.54, 9.62, 9.49, 9.59)$. What is the posterior distribution for $\theta$?

**Question 18.** Scientist A studies the probability of a correct detection in a psychophysics experiment and calls it $\theta$. To be non-informative, she assumes a $\text{Uniform}(0,1)$ prior for $\theta$. Scientist B studies the same experiment using the same data, but wishes to model the odds

$$
\phi = \frac{\theta}{1 - \theta}
$$

Scientist B places a uniform distribution on $\phi$. If she reports her inferences in terms of the probability $\theta$, will they be equivalent to the inferences made by Scientist A?
