# Lesson 2: How should evidence change our expectations?

In [Lesson 1](1.ResearchQuestionsDataMeasurement.md), we specified what we want to learn and what the data represent. Now we ask how observations change uncertainty about that quantity. The central idea is **prior information × compatibility with the new data → updated uncertainty**. Bayes' rule tells us how to combine those ingredients mathematically.

## Learning objectives

By the end of this lesson, you should be able to:

1. Understand conditional probability and how to calculate it using the product and sum rules.
2. Identify and explain the components of Bayes Theorem (prior, likelihood, posterior, marginal likelihood).
3. Apply Bayes Theorem to interactive examples (like disease testing and the Monty Hall paradox) to observe probability updates in practice.
4. Distinguish between a probability density and a likelihood function.

:::{tip}
**Companion workbook**

The [existing Colab workbook](https://colab.research.google.com/github/iknyazeva/bayes-cogsci-book/blob/main/notebooks/colab/02_probability_bayesian_updating.ipynb) contains conditional-probability calculations, interactive updating examples, and the core exercises.
:::

## 1. The Rules of Probability

```{image} CatsDice.jpeg
:alt: Cats and Dice Probability
:width: 280px
:align: center
```

Before we can update beliefs with data, we need a mathematical language for uncertainty. The foundation of probability relies on two core rules.

### The Product Rule (Joint Probability)
The probability that both proposition $A$ and proposition $B$ occur together (their joint probability) is:

$$\mathbb{P}(A, B) = \mathbb{P}(A \mid B)\mathbb{P}(B) = \mathbb{P}(B \mid A)\mathbb{P}(A)$$

Here, $\mathbb{P}(A \mid B)$ is the **conditional probability** of $A$ given that $B$ is true. 

### The Sum Rule (Marginal Probability)
If a space is partitioned into mutually exclusive and exhaustive possibilities $\{B_1, B_2, \dots, B_K\}$, the total (marginal) probability of $A$ is the sum of its joint probabilities across all possible states of $B$:

$$\mathbb{P}(A) = \sum_{k=1}^K \mathbb{P}(A, B_k) = \sum_{k=1}^K \mathbb{P}(A \mid B_k)\mathbb{P}(B_k)$$

```{image} PriorsDrawio.png
:alt: Prior Hypotheses and Evidence Updating
:width: 600px
:align: center
```
*Figure 1.1: The Intuition of Prior Hypotheses and Evidence Updating. Before observing new data, several prior hypotheses are plausible. When evidence arrives, the likelihood reweights the proposed explanations according to how well they predict that evidence.*

## 2. Bayes' Theorem and The False Positive Paradox

If we take the symmetry of the product rule:
$$\mathbb{P}(H \mid D)\mathbb{P}(D) = \mathbb{P}(D \mid H)\mathbb{P}(H)$$

And divide both sides by $\mathbb{P}(D)$, we get **Bayes' Theorem**:

$$\mathbb{P}(H \mid D) = \frac{\mathbb{P}(D \mid H)\mathbb{P}(H)}{\mathbb{P}(D)}$$

Where:
* **Prior $\mathbb{P}(H)$**: Our belief in the hypothesis before seeing the data.
* **Likelihood $\mathbb{P}(D \mid H)$**: How probable the observed data is, assuming the hypothesis is true.
* **Marginal Likelihood $\mathbb{P}(D)$**: The total probability of observing the data across all hypotheses (calculated via the Sum Rule).
* **Posterior $\mathbb{P}(H \mid D)$**: Our updated belief in the hypothesis after seeing the data.

### The False Positive Paradox
Consider an automated quality check that flags corrupted recordings. 
* **Prior (Base Rate)**: 1% of recordings are corrupted. $\mathbb{P}(C) = 0.01$
* **Likelihood (Sensitivity)**: The system correctly flags 95% of corrupted recordings. $\mathbb{P}(F \mid C) = 0.95$
* **Likelihood (False Positives)**: The system incorrectly flags 10% of usable recordings. $\mathbb{P}(F \mid \neg C) = 0.10$

If a recording is flagged, what is the probability it is actually corrupted? $\mathbb{P}(C \mid F)$

Using Bayes Theorem:
$$\mathbb{P}(C \mid F) = \frac{\mathbb{P}(F \mid C)\mathbb{P}(C)}{\mathbb{P}(F \mid C)\mathbb{P}(C) + \mathbb{P}(F \mid \neg C)\mathbb{P}(\neg C)}$$
$$\mathbb{P}(C \mid F) = \frac{0.95 \times 0.01}{(0.95 \times 0.01) + (0.10 \times 0.99)} = \frac{0.0095}{0.0095 + 0.0990} \approx 8.76\%$$

**The Inverse Probability Fallacy**: Conflating the likelihood $\mathbb{P}(F \mid C) = 95\%$ with the posterior $\mathbb{P}(C \mid F) = 8.76\%$ is one of the most common errors in empirical science (often illustrated with rare disease or HIV screening). When a condition is rare (a low prior base rate), false positives from the large healthy population easily overwhelm true positives.

*Figure 2.2: Posterior Probability of Condition $\mathbb{P}(C \mid +)$ as a function of population prevalence. Notice how the identical positive test result yields completely different posterior probabilities depending on the prior base rate of the population.*

<div style="width: 100%; min-height: 400px; margin: 1.25rem 0;">
  <iframe src="../_static/interactive_prevalence_curve.html" width="100%" height="400" style="border:none; border-radius:8px; width:100%; height:400px; min-height:400px;"></iframe>
</div>

*Interactive Natural Frequency & Base-Rate Explorer: Click the buttons above the plot to observe how base rate prevalence shifts the ratio of True Positives to False Positives out of 10,000 tested individuals:*

<div style="width: 100%; min-height: 500px; margin: 1.25rem 0;">
  <iframe src="../_static/interactive_base_rates.html?v=3" width="100%" height="500" style="border:none; border-radius:8px; width:100%; height:500px; min-height:500px;"></iframe>
</div>

## 3. The Monty Hall Paradox

The Monty Hall problem is a famous test of Bayesian reasoning.
1. You are on a game show. There are 3 doors. Behind one is a car; behind the other two are goats.
2. You pick a door (say, Door 1).
3. The host, who knows what is behind the doors, opens another door (say, Door 2) to reveal a goat.
4. He asks: "Do you want to switch to Door 3?"

Should you switch? Bayes' Theorem gives us the exact answer by forcing us to define the likelihood of the *host's behavior*.

* **Prior**: Before any doors are opened, the car is equally likely to be behind any door. $\mathbb{P}(D_1) = \mathbb{P}(D_2) = \mathbb{P}(D_3) = 1/3$.
* **Likelihood**: You picked Door 1. What is the likelihood the host opens Door 2?
  - If the car is behind Door 1, the host can open 2 or 3 randomly. $\mathbb{P}(\text{Host 2} \mid \text{Car 1}) = 1/2$.
  - If the car is behind Door 2, the host *cannot* open Door 2. $\mathbb{P}(\text{Host 2} \mid \text{Car 2}) = 0$.
  - If the car is behind Door 3, the host *must* open Door 2 (since you picked 1). $\mathbb{P}(\text{Host 2} \mid \text{Car 3}) = 1$.

*Figure 2.1: Bayesian Updating in the Monty Hall Problem. Notice how the likelihood heavily favors the hypothesis that the car is behind Door 3, shifting the posterior probability to 2/3.*

<iframe src="../_static/monty_hall_demo.html" width="100%" height="450px" style="border:none; border-radius:8px;"></iframe>

By switching to Door 3, you double your chances of winning!

## 4. Likelihood vs. Probability & Continuous Updating

We've looked at discrete hypotheses (Corrupted/Usable, Door 1/2/3). The same principles apply to continuous parameters, like an unknown success probability $\theta$. 

### Likelihood is NOT a Probability Distribution
The likelihood function $\mathcal{L}(\theta \mid D) = \mathbb{P}(D \mid \theta)$ evaluates the compatibility of different candidate parameter values $\theta$ for a *fixed* observed dataset $D$. 

It is an index of relative compatibility. It is **not** required to be normalized over parameters (its integral does not have to equal 1), which is why we must multiply it by a prior and normalize it to get a valid posterior probability distribution $p(\theta \mid D)$.

*Figure 2.4: The Duality of Likelihood and Probability. Panel (a) shows probability density for fixed parameter. Panel (b) shows the likelihood function for a fixed observation, which does not integrate to 1.*

<div style="width: 100%; min-height: 600px; margin: 1.5rem 0;">
  <iframe src="../_static/fig2_4_likelihood_vs_density.html" width="100%" height="600" style="border:none; border-radius:8px; width:100%; height:600px; min-height:600px;"></iframe>
</div>

### Sequential Updating
Bayesian learning is naturally cumulative: yesterday's posterior becomes today's prior. 

If we use a **Beta distribution** for our prior over $\theta$ and observe binary (Bernoulli) successes and failures, the math is perfectly conjugate. 

$$ \operatorname{Beta}(\alpha, \beta) \text{ prior} + k \text{ successes and } (N-k) \text{ failures} \longrightarrow \operatorname{Beta}(\alpha+k, \beta+N-k) \text{ posterior} $$

*Figure 2.2: Step-by-Step Bernoulli Updating Triad. Click the buttons to follow sequential coin flips ($y_1 = 1, y_2 = 0, y_3 = 1, y_4 = 1, y_5 = 0$). Watch how each step's posterior becomes the prior for the next observation.*

<iframe src="../_static/fig2_3_sequential_bernoulli_triad.html" width="100%" height="360px" style="border:none; border-radius:8px;"></iframe>

Updating your beliefs sequentially produces the exact same posterior distribution as updating on all batch data at once.

## 5. Guided Practice

### Exercise 1: Change the denominator
Using the quality-check table from Section 2, calculate the probability that a recording is corrupted **given that it is not flagged**. 

:::{dropdown} Answer
The denominator is the 8,915 unflagged recordings. Of these, 5 are corrupted: $5/8{,}915\approx0.000561$, or **0.0561%**. This differs from $P(\text{not flagged}\mid C)=5/100=5\%$, which uses only corrupted recordings as its denominator.
:::

### Exercise 2: Predict an update
Starting from a $\operatorname{Beta}(2,2)$ prior, you observe 3 correct responses in 5 independent comparable observations. State the posterior and its mean.

:::{dropdown} Answer
The posterior is $\operatorname{Beta}(2+3, 2+2) = \operatorname{Beta}(5,4)$, with mean $5/9\approx0.556$. The prior centered at 0.5 pulls the posterior mean below the observed 0.6 proportion.
:::

### Exercise 3: Bayesian Updating with Different Beta Priors
You observe a sample of $N = 5$ coin tosses resulting in $k = 3$ Heads and $2$ Tails. Compare how this identical evidence updates 4 different prior beliefs about coin bias $\theta$. 

*Click the buttons to toggle each Beta prior and compare all 4 posteriors overlaid:*

<div style="width: 100%; min-height: 450px; margin: 1.25rem 0;">
  <iframe src="../_static/interactive_sequential_updating.html?v=3" width="100%" height="450" style="border:none; border-radius:8px; width:100%; height:450px; min-height:450px;"></iframe>
</div>

:::{dropdown} Answer
Bayesian updating acts as a **weighted compromise** between the prior mean and the sample evidence. 
1. **Uniform $\operatorname{Beta}(1,1)$**: Posterior $\operatorname{Beta}(4,3)$. The mean (0.571) is pulled slightly toward 0.5 from the sample proportion (0.6).
2. **Fair-Coin $\operatorname{Beta}(10,10)$**: Posterior $\operatorname{Beta}(13,12)$. The strong prior resists the 5 flips, shifting the mean only to 0.520.
3. **Heads-Biased $\operatorname{Beta}(8,2)$**: Posterior $\operatorname{Beta}(11,4)$. The lower data proportion (0.6) pulls the prior mean (0.8) down to 0.733.
4. **Tails-Biased $\operatorname{Beta}(2,8)$**: Posterior $\operatorname{Beta}(5,10)$. The higher data proportion (0.6) pulls the prior mean (0.2) up to 0.333.
:::

## 6. Self-Assessment Quizzes

These quizzes cover the notation, random variables, and Bayesian updating concepts. Try solving them before checking the answer keys at the bottom of each section.

### Quiz 1: Notation, Definitions, and Distribution Properties



```{admonition} Instructions
Answer each question. Show your calculations where applicable. Select the correct option(s) for multiple-choice questions.
```

#### Notation and Definitions

**Question 1.**
When using random variable notation, big $X$ denotes ________.

1. a random variable
2. a conditional probability
3. distributed as
4. a realization of a random variable
5. the expectation of a random variable
6. approximately equal to

**Question 2.**
When using random variable notation, little $x$ denotes ________.

1. a random variable
2. a conditional probability
3. distributed as
4. a realization of a random variable
5. the expectation of a random variable
6. approximately equal to

**Question 3.**
When using random variable notation, $X \sim$ denotes ________.

1. a random variable
2. a conditional probability
3. distributed as
4. a realization of a random variable
5. the expectation of a random variable
6. approximately equal to

**Question 4.**
What is the value of $f(x) = -5 I_{\{x > 2\}}(x) + x I_{\{x < -1\}}(x)$ when $x = 0$?

**Question 5.**
What is the value of $f(x) = -5 I_{\{x > 2\}}(x) + x I_{\{x < -1\}}(x)$ when $x = -2$?

**Question 6.**
Which of the following scenarios could we appropriately model using a Bernoulli random variable?

1. Predicting whether a participant correctly detects a target in a visual search trial (yes/no)
2. Predicting the reaction time of a participant in a Stroop task
3. Predicting the number of errors a participant makes in a 50-trial memory task
4. Predicting the number of correct responses out of 20 trials in a signal detection experiment
5. Predicting whether a participant correctly identifies a briefly flashed letter (yes/no)

**Question 7.**
Calculate the expected value of the following random variable: $X$ takes on values $\{0, 1, 2, 3\}$ with corresponding probabilities $\{0.5, 0.2, 0.2, 0.1\}$. Round your answer to one decimal place.

**Question 8.**
Which of the following scenarios could we appropriately model using a binomial random variable (with $n > 1$)?

1. Predicting whether a participant correctly detects a target in a single visual search trial (yes/no)
2. Predicting the reaction time of a participant in a Stroop task
3. Predicting the number of action potentials a neuron fires in one second
4. Predicting the number of correct identifications out of 10 face recognition trials
5. Predicting the number of correctly identified items out of 20 briefly flashed letters

**Question 9.**
Suppose $X \sim \text{Binomial}(3, 0.2)$. Calculate $P(X = 0)$. Round your answer to two decimal places.

**Question 10.**
Suppose $X \sim \text{Binomial}(3, 0.2)$. Calculate $P(X \leq 2)$. Round your answer to two decimal places.


#### Random Variables and Distribution Properties

**Question 11.**
If continuous random variable $X$ has probability density function (PDF) $f(x)$, what is the interpretation of the following integral: $\int_{-2}^{5} f(x) \, dx$ ?

1. $P(X \geq -2 \cap X \leq 5)$
2. $P(X \geq -2 \cup X \leq 5)$
3. $P(X \leq -2 \cap X \geq 5)$

**Question 12.**
If $X \sim \text{Uniform}(0, 1)$, then what is the value of $P(-3 < X < 0.2)$?

**Question 13.**
If $X \sim \text{Exponential}(5)$, find the expected value $E(X)$.

**Question 14.**
Which of the following scenarios could we most appropriately model using an exponentially distributed random variable?

1. The total time until all participants in a group of 50 complete a cognitive task
2. The number of attention lapses a participant makes during a 30-minute vigilance task
3. The probability that a participant's reaction time exceeds 500 ms on a given trial
4. The waiting time (in seconds) until a participant's next fixation during free viewing of a scene

**Question 15.**
If $X \sim \text{Uniform}(2, 6)$, what is $P(2 < X \leq 3)$? Round your answer to two decimal places.

**Question 16.**
If $X \sim N(2, 1)$, what is the expected value of $-5X$? This is denoted as $E(-5X)$.

**Question 17.**
Let $X \sim N(1, 1)$ and $Y \sim N(4, 3^2)$. What is the value of $E(X + Y)$?

**Question 18.**
The normal distribution is also linear in the sense that if $X \sim N(\mu, \sigma^2)$, then for any real constants $a \neq 0$ and $b$, the distribution of $Y = aX + b$ is $N(a\mu + b, \; a^2 \sigma^2)$.

Using this property: if $X \sim N(3, 4)$, what is the distribution of $Y = 2X - 1$?

**Question 19.**
Which of the following random variables would yield the highest value of $P(-1 < X < 1)$?

1. $X \sim N(0, 0.1)$
2. $X \sim N(0, 1)$
3. $X \sim N(0, 10)$
4. $X \sim N(0, 100)$

**Question 20.**
Which of the following (possibly more than one) must be true if random variable $X$ is continuous with PDF $f(x)$?

- $f(x)$ is an increasing function of $x$
- $\int_{-\infty}^{\infty} f(x) \, dx = 1$
- $f(x)$ is a continuous function
- $X \geq 0$ always
- $f(x) \geq 0$ always
- $\lim_{x \to \infty} f(x) = \infty$

**Question 21.**
If $X \sim \text{Exp}(3)$, what is the value of $P(X > 1/3)$? Round your answer to two decimal places.

**Question 22.**
Suppose $X \sim \text{Uniform}(0, 2)$ and $Y \sim \text{Uniform}(8, 10)$. What is the value of $E(4X + Y)$?

**Question 23.**
Suppose $X \sim N(1, 5^2)$ and $Y \sim N(-2, 3^2)$ and that $X$ and $Y$ are independent. We have $Z = X + Y \sim N(\mu, \sigma^2)$ because the sum of normal random variables also follows a normal distribution.

- What is the value of $\mu$?
- What is the value of $\sigma^2$?

**Question 24.**
If random variables $X$ and $Y$ are not independent, we still have $E(X + Y) = E(X) + E(Y)$, but now $\text{Var}(X + Y) = \text{Var}(X) + \text{Var}(Y) + 2\text{Cov}(X, Y)$ where $\text{Cov}(X, Y) = E[(X - E[X])(Y - E[Y])]$ is called the covariance between $X$ and $Y$.

A convenient formula for calculating variance is: $\text{Var}(X) = E[(X - E[X])^2] = E[X^2] - (E[X])^2$. Which of the following is an analogous expression for the covariance of $X$ and $Y$?

1. $E[Y^2] - (E[Y])^2$
2. $E[X^2] - (E[X])^2 + E[Y^2] - (E[Y])^2$
3. $(E[X^2] - (E[X])^2) \cdot (E[Y^2] - (E[Y])^2)$
4. $E(XY) - E(X)E(Y)$

Consider again $X \sim N(1, 5^2)$ and $Y \sim N(-2, 3^2)$, but this time $X$ and $Y$ are _not_ independent. Then $Z = X + Y$ is still normally distributed with the same mean found in Question 23. What is the variance of $Z$ if $E(XY) = -5$?

**Question 25.**
A researcher measures reaction times (in seconds) in a sustained attention task. The times between consecutive lapses of attention follow an Exponential distribution with rate $\lambda = 2$ per minute.

- What is the expected time between attention lapses?
- What is the probability that a participant goes more than 1 minute without an attention lapse?


:::{dropdown} Answer Key for Quiz 1
- **Q1**: 1 (a random variable)
- **Q2**: 4 (a realization of a random variable)
- **Q3**: 3 (distributed as)
- **Q4**: 0
- **Q5**: -2
- **Q6**: 1, 5
- **Q7**: 0.9
- **Q8**: 4, 5
- **Q9**: 0.51
- **Q10**: 0.99
- **Q11**: 1
- **Q12**: 0.2
- **Q13**: 1/5 or 0.2
- **Q14**: 4
- **Q15**: 0.25
- **Q16**: -10
- **Q17**: 5
- **Q18**: $N(5, 16)$
- **Q19**: 1
- **Q20**: $\int_{-\infty}^{\infty} f(x) \, dx = 1$ and $f(x) \geq 0$ always
- **Q21**: 0.37
- **Q22**: 13
- **Q23**: $\mu = -1$, $\sigma^2 = 34$
- **Q24**: Formula is 4 ($E(XY) - E(X)E(Y)$). Variance of Z is 28.
- **Q25**: Expected time = 0.5 min (30s). Probability = 0.135 ($\approx e^{-2}$).
:::

### Quiz 2: Bayesian Inference



```{admonition} Instructions
For each question, show your work. Identify the prior, likelihood, and posterior where applicable.
```

#### Political Preferences

**For Questions 1–5, consider the following scenario:**

You are trying to ascertain your American colleague's political preferences. To do so, you design a questionnaire with five yes/no questions relating to current issues. The questions are all worded so that a "yes" response indicates a conservative viewpoint.

Let $\theta$ be the unknown political viewpoint of your colleague, which we will assume can only take values $\theta = \text{conservative}$ or $\theta = \text{liberal}$. You have no reason to believe that your colleague leans one way or the other, so you assign the prior $P(\theta = \text{conservative}) = 0.5$.

**Question 1.** Assume the five questions are independent and let $Y$ count the number of "yes" responses. If your colleague is conservative, then the probability of a "yes" response on any given question is 0.8. If your colleague is liberal, the probability of a "no" response on any given question is 0.7. Which of the following is the correct likelihood?

1. $f(y \mid \theta) = \frac{\theta^y e^{-\theta}}{y!}$
2. $f(y \mid \theta) = \binom{5}{y} 0.8^y 0.2^{5-y}$
3. $f(y \mid \theta) = \binom{5}{y} 0.8^y 0.2^{5-y} I_{\theta = \text{conservative}} + \binom{5}{y} 0.3^y 0.7^{5-y} I_{\theta = \text{liberal}}$
4. $f(y \mid \theta) = \binom{5}{y} 0.3^y 0.7^{5-y} I_{\theta = \text{conservative}} + \binom{5}{y} 0.8^y 0.2^{5-y} I_{\theta = \text{liberal}}$

**Question 2.** Suppose you ask your colleague the five questions and he answers "no" to all of them. What is the MLE for $\theta$?

1. $\hat{\theta} = \text{conservative}$
2. $\hat{\theta} = \text{liberal}$
3. None of the above. The MLE is a number.

**Question 3.** What is the corresponding expression for the posterior probability?

1. $f(\theta \mid y) = \frac{\binom{5}{y} 0.8^y 0.2^{5-y}(0.2) I_{\theta=\text{conservative}} + \binom{5}{y} 0.3^y 0.7^{5-y}(0.7) I_{\theta=\text{liberal}}}{\binom{5}{y} 0.8^y 0.2^{5-y}(0.2) + \binom{5}{y} 0.3^y 0.7^{5-y}(0.7)}$
2. $f(\theta \mid y) = \frac{\binom{5}{y} 0.8^y 0.2^{5-y}(0.5) I_{\theta=\text{conservative}} + \binom{5}{y} 0.3^y 0.7^{5-y}(0.5) I_{\theta=\text{liberal}}}{\binom{5}{y} 0.8^y 0.2^{5-y}(0.5) + \binom{5}{y} 0.3^y 0.7^{5-y}(0.5)}$
3. $f(\theta \mid y) = \frac{\binom{5}{y} 0.8^y 0.2^{5-y}(0.2)(0.5)^2}{\binom{5}{y} 0.8^y 0.2^{5-y}(0.5) + \binom{5}{y} 0.3^y 0.7^{5-y}(0.5)}$
4. $f(\theta \mid y) = \frac{\binom{5}{y} 0.8^y 0.2^{5-y}(0.5) + \binom{5}{y} 0.3^y 0.7^{5-y}(0.5)}{\binom{5}{y} 0.8^y 0.2^{5-y}(0.5) + \binom{5}{y} 0.3^y 0.7^{5-y}(0.5)}$

**Question 4.** Evaluate the expression in Question 3 for $y = 0$ and report the posterior probability that your colleague is conservative, given that he responded "no" to all of the questions. Round your answer to three decimal places.

**Question 5.** Evaluate the expression in Question 3 for $y = 0$ and report the posterior probability that your colleague is liberal, given that he responded "no" to all of the questions. Round your answer to three decimal places.


#### Loaded Coins

**Question 6.** Recall that your brother has a fair coin which comes up heads 50% of the time and a loaded coin which comes up heads 70% of the time. Suppose now that he has a third coin which comes up tails 70% of the time. Again, you don't know which coin your brother has brought you, so you are going to test it by flipping it 4 times, where $X$ counts the number of heads.

Let $\theta$ identify the coin so that there are three possibilities: $\theta = \text{fair}$, $\theta = \text{loaded\_heads}$, and $\theta = \text{loaded\_tails}$.

Suppose the prior is: $P(\theta = \text{fair}) = 0.4$, $P(\theta = \text{loaded\_heads}) = 0.3$, $P(\theta = \text{loaded\_tails}) = 0.3$.

Our prior probability that the coin is loaded is still 0.6, but we do not know which loaded coin it is, so we split the probability evenly between the two options. What is the form of the likelihood?

1. $f(x \mid \theta) = \binom{4}{x} 0.5^4 I_{\theta=\text{fair}} + \binom{4}{x} 0.7^x 0.3^{4-x} I_{\theta=\text{loaded\_heads}} + \binom{4}{x} 0.3^x 0.7^{4-x} I_{\theta=\text{loaded\_tails}}$
2. $f(x \mid \theta) = \binom{4}{x} 0.5^4 I_{\theta=\text{fair}} + \binom{4}{x} 0.3^x 0.7^{4-x} I_{\theta=\text{loaded\_heads}} + \binom{4}{x} 0.7^x 0.3^{4-x} I_{\theta=\text{loaded\_tails}}$
3. $f(x \mid \theta) = \binom{4}{x} 0.5^4 (0.4) I_{\theta=\text{fair}} + \binom{4}{x} 0.3^x 0.7^{4-x} (0.3) I_{\theta=\text{loaded\_heads}} + \binom{4}{x} 0.7^x 0.3^{4-x} (0.3) I_{\theta=\text{loaded\_tails}}$

**Question 7.** Suppose you flip the coin four times and it comes up heads twice. What is the MLE for $\theta$?

1. $\hat{\theta} = \text{fair}$
2. $\hat{\theta} = \text{loaded\_heads}$
3. $\hat{\theta} = \text{loaded\_tails}$
4. None of the above. The MLE is a number.

**Question 8.** Suppose you flip the coin four times and it comes up heads twice. What is the posterior probability that this is the fair coin? Round your answer to two decimal places.

**Question 9.** Suppose you flip the coin four times and it comes up heads twice. What is the posterior probability that this is a loaded coin (favoring either heads or tails)? Round your answer to two decimal places.


#### Signal Detection in a Psychophysics Experiment

A participant performs a visual detection task. On each trial, a faint stimulus is presented and the participant reports whether they saw it (yes/no). You model their detection rate $\theta$ with a uniform prior $\text{Beta}(1, 1)$ on $[0, 1]$.

In 20 trials, the participant detects the stimulus 14 times.

**Question 10.** Write the likelihood function for this data, treating each trial as an independent Bernoulli observation with success probability $\theta$.

**Question 11.** What is the MLE for $\theta$?

**Question 12.** What is the posterior distribution for $\theta$? (Hint: use the Beta-Binomial conjugacy — see {doc}`4.ConjugatePriors` for the Beta distribution and conjugacy rules.)

**Question 13.** Compute the posterior mean and compare it to the MLE. Why are they different? In which direction does the prior "pull" the estimate, and why?


:::{dropdown} Answer Key for Quiz 2
- **Q1**: 3. The likelihood conditions on $\theta$, using indicator functions to select the correct Bernoulli probability.
- **Q2**: 2. $\hat{\theta} = \text{liberal}$ (0.168 > 0.00032).
- **Q3**: 2. This represents Prior × Likelihood / Marginal.
- **Q4**: 0.002 ($0.00032 / (0.00032 + 0.16807)$).
- **Q5**: 0.998.
- **Q6**: 1. (Option 3 multiplies by the prior, which makes it the joint distribution, not the likelihood).
- **Q7**: 1. (Fair coin likelihood is 0.0625, loaded is 0.0441).
- **Q8**: 0.49 ($0.025 / 0.05146$).
- **Q9**: 0.51.
- **Q10**: $L(\theta) = \theta^{14} (1-\theta)^6$
- **Q11**: $\hat{\theta} = 14/20 = 0.7$
- **Q12**: $\text{Beta}(15, 7)$ (Using Beta-Binomial conjugacy: $\alpha+14, \beta+6$).
- **Q13**: Posterior mean = $15/22 \approx 0.68$. The uniform prior acts as two pseudo-observations (one success, one failure), pulling the MLE (0.70) slightly downward toward 0.5.
:::

## 7. Research protocol milestone 2

Add an **ungraded working record** to your project:
1. **Unknown quantity:** What parameter or event are you uncertain about, and what values are possible?
2. **Prior information:** What information is available before using the current data?
3. **Evidence:** What observations will update the quantity, and how are they counted?
4. **Expected update:** Describe evidence that would move your expectation upward or downward.

## References

```{bibliography}
:filter: docname in docnames
```
